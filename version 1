-- Core Services
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer or Players.PlayerAdded:Wait()
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local TextChatService = game:GetService("TextChatService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")


-- Intro Screen Overlay Function
local function playIntroScreen()
    local sg = Instance.new("ScreenGui")
    sg.Name = "SavenHub🔥_Intro"
    sg.ResetOnSpawn = false
    sg.Parent = (gethui and gethui()) or CoreGui or LocalPlayer:WaitForChild("PlayerGui")

    -- Main Card Container
    local card = Instance.new("Frame")
    card.Size = UDim2.new(0, 420, 0, 240)
    card.Position = UDim2.new(0.5, -210, 0.5, -120)
    card.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    card.BackgroundTransparency = 0.25
    card.BorderSizePixel = 0
    card.Parent = sg

    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 30)
    cardCorner.Parent = card

    -- Outer Purple Glow Ring
    local outerGlow = Instance.new("UIStroke")
    outerGlow.Color = Color3.fromRGB(160, 32, 240)
    outerGlow.Thickness = 6
    outerGlow.Transparency = 0.3
    outerGlow.Parent = card

    -- Inner Purple Border
    local innerBorder = Instance.new("UIStroke")
    innerBorder.Color = Color3.fromRGB(200, 50, 255)
    innerBorder.Thickness = 2
    innerBorder.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    innerBorder.Parent = card

    -- Avatar Headshot Image
    local avatarFrame = Instance.new("Frame")
    avatarFrame.Size = UDim2.new(0, 80, 0, 80)
    avatarFrame.Position = UDim2.new(0.5, -40, 0.1, 0)
    avatarFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    avatarFrame.Parent = card

    local avatarCorner = Instance.new("UICorner")
    avatarCorner.CornerRadius = UDim.new(1, 0)
    avatarCorner.Parent = avatarFrame

    local avatarStroke = Instance.new("UIStroke")
    avatarStroke.Color = Color3.fromRGB(180, 50, 255)
    avatarStroke.Thickness = 3
    avatarStroke.Parent = avatarFrame

    local avatarImg = Instance.new("ImageLabel")
    avatarImg.Size = UDim2.new(1, 0, 1, 0)
    avatarImg.BackgroundTransparency = 1
    pcall(function()
        avatarImg.Image = Players:GetUserThumbnailAsync(LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size150x150)
    end)
    avatarImg.Parent = avatarFrame

    local imgCorner = Instance.new("UICorner")
    imgCorner.CornerRadius = UDim.new(1, 0)
    imgCorner.Parent = avatarImg

    -- Main Title Text
    local titleText = Instance.new("TextLabel")
    titleText.Size = UDim2.new(1, 0, 0, 30)
    titleText.Position = UDim2.new(0, 0, 0.5, 0)
    titleText.BackgroundTransparency = 1
    titleText.Text = "SavenHub🔥"
    titleText.TextColor3 = Color3.fromRGB(255, 255, 255)
    titleText.TextSize = 22
    titleText.Font = Enum.Font.GothamBold
    titleText.Parent = card

    -- Subtitle Loading / Welcome Text
    local statusText = Instance.new("TextLabel")
    statusText.Size = UDim2.new(1, 0, 0, 25)
    statusText.Position = UDim2.new(0, 0, 0.72, 0)
    statusText.BackgroundTransparency = 1
    statusText.Text = "AUTHENTICATING SavenHub..."
    statusText.TextColor3 = Color3.fromRGB(180, 50, 255)
    statusText.TextSize = 14
    statusText.Font = Enum.Font.Code
    statusText.Parent = card

    -- Animation sequence
    task.wait(1.5)
    statusText.Text = "WELCOME, " .. string.upper(LocalPlayer.Name) .. " To SavenHub 🔥"
    statusText.TextColor3 = Color3.fromRGB(255, 0, 255)
    task.wait(1.5)

    -- Fade out
    local fade = TweenService:Create(card, TweenInfo.new(0.6), {BackgroundTransparency = 1})
    TweenService:Create(titleText, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
    TweenService:Create(statusText, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
    TweenService:Create(avatarImg, TweenInfo.new(0.6), {ImageTransparency = 1}):Play()
    TweenService:Create(outerGlow, TweenInfo.new(0.6), {Transparency = 1}):Play()
    TweenService:Create(innerBorder, TweenInfo.new(0.6), {Transparency = 1}):Play()
    TweenService:Create(avatarStroke, TweenInfo.new(0.6), {Transparency = 1}):Play()
    fade:Play()
    fade.Completed:Wait()
    sg:Destroy()
end

-- Play Intro First
playIntroScreen()

-- Load Rayfield UI Library
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Create Main Window
local Window = Rayfield:CreateWindow({
   Name = "SavenHub🔥",
   LoadingTitle = "SavenHub🔥",
   LoadingSubtitle = "Created by {SHEHRYAR x KAISER}",
   Theme = "Default",

   ConfigurationSaving = {
      Enabled = true,
      FolderName = "SavenHub Configuration",
      FileName = "MainSettings"
   },

   Discord = {
      Enabled = true,
      Invite = "TsVVgctW",
      RememberJoins = true
   },

   KeySystem = true,
   KeySettings = {
      Title = "SavenHub🔥",
      Subtitle = "Key System",
      Note = "Join our Discord (discord.gg/TsVVgctW) to get the key!",
      FileName = "Savenhub key",
      SaveKey = true,
      GrabKeyFromSite = false,
      Key = {"KING_KAISER", "SUPREME_AURA350", "SavenHub1213"}
   }
})

-- Create Main Tab
local MainTab = Window:CreateTab("Player Features", "user")

-- Create Section
MainTab:CreateSection("Movement Modifications")

-- 1. Button
MainTab:CreateButton({
   Name = "Print Speed",
   Callback = function()
       if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
           print("Current WalkSpeed: " .. LocalPlayer.Character.Humanoid.WalkSpeed)
       end
   end,
})

-- 2. Toggle Speed
MainTab:CreateToggle({
   Name = "Super Speed",
   CurrentValue = false,
   Flag = "SpeedToggle1",
   Callback = function(Value)
       if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
           LocalPlayer.Character.Humanoid.WalkSpeed = Value and 50 or 16
       end
   end,
})

-- 3. Slider Jump Power
MainTab:CreateSlider({
   Name = "Jump Power",
   Range = {50, 200},
   Increment = 5,
   Suffix = " Power",
   CurrentValue = 50,
   Flag = "JumpSlider1",
   Callback = function(Value)
       if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
           LocalPlayer.Character.Humanoid.JumpPower = Value
       end
   end,
})

-- Target Player Input
_G.TargetPlayer = ""
MainTab:CreateInput({
   Name = "Target Player Name",
   PlaceholderText = "Type target name...",
   RemoveTextAfterFocusLost = false,
   Callback = function(Text)
       _G.TargetPlayer = Text
   end,
})

-- 4. Spammer Toggle
MainTab:CreateToggle({
   Name = "SavenHub SPAMMER",
   CurrentValue = false,
   Flag = "SpammerToggle",
   Callback = function(value)
       _G.LoopMessages = value

       if _G.LoopMessages then
           task.spawn(function()
               local messages = {
                   " TMKX MEH DARK POWER KAISER VOID WORM WAVE GOD FACT WAVE PRIME PARALLEL PRIME(🤣)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@",
                   " TMKX MEH Bicycle(🙈) | TBKX MARE Smart Phone(☄️) |  TMKX MARE Desk(🥀)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@",
                   " TMKX MEH KAISER WORM PARALLEL GOD WAVE HYPER VOID KAISER WAVE WAVE(😝)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@",
                   " TMKX MEH KAISER WORM PARALLEL LEGEND WAVE HYPER VOID KAISER WAVE POWER(💥)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@",
                   " TMKX MEH Coffee Mug(⭐) | TMKX MARE Notebook(💎) | TBKX KUDAI Ceiling Fan(🔥)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@"
               }

               local msgIndex = 1
               while _G.LoopMessages do
                   local target = (_G.TargetPlayer and _G.TargetPlayer ~= "") and _G.TargetPlayer or "(💗KAISER💗)H8RS(😝)"
                   local fullMsg = target .. messages[msgIndex]

                   pcall(function()
                       if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
                           TextChatService.TextChannels.RBXGeneral:SendAsync(fullMsg)
                       else
                           ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(fullMsg, "All")
                       end
                   end)

                   msgIndex = (msgIndex % #messages) + 1
                   task.wait(2)
               end
           end)
       end
   end,
})

-- Socials Section
MainTab:CreateSection("Socials & Credits")

MainTab:CreateButton({
   Name = "JOIN THE SERVER",
   Callback = function()
       if setclipboard then
           setclipboard("https://discord.gg/TsVVgctW")
       end
       Rayfield:Notify({
           Title = "Creator's Discord Invite",
           Content = "Copied the server link to clipboard!",
           Duration = 4,
           Image = 4483362458,
       })
   end,
})

MainTab:CreateButton({
   Name = "JOIN 2nd Creator's Server", 
   Callback = function()
       if setclipboard then
           setclipboard("mrshehryarlegend92")
       end
       Rayfield:Notify({
           Title = "2nd Creator's Discord Invite",
           Content = "Copied the Server Username to clipboard!",
           Duration = 4,
           Image = 4483362458,
       })
   end,
})

-- Glowing Window Border Task
task.spawn(function()
    local rayfieldGui = CoreGui:WaitForChild("Rayfield", 10) or LocalPlayer:WaitForChild("PlayerGui"):WaitForChild("Rayfield", 10)
    
    if rayfieldGui then
        local mainFrame = rayfieldGui:WaitForChild("Main", 10)
        if mainFrame then
            local stroke = Instance.new("UIStroke")
            stroke.Name = "GlowStroke"
            stroke.Color = Color3.fromRGB(170, 85, 255)
            stroke.Thickness = 2
            stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
            stroke.Parent = mainFrame

            local glowImage = Instance.new("ImageLabel")
            glowImage.Name = "GlowShadow"
            glowImage.BackgroundTransparency = 1
            glowImage.Position = UDim2.new(0, -15, 0, -15)
            glowImage.Size = UDim2.new(1, 30, 1, 30)
            glowImage.ZIndex = mainFrame.ZIndex - 1
            glowImage.Image = "rbxassetid://5028857472"
            glowImage.ImageColor3 = Color3.fromRGB(170, 85, 255)
            glowImage.ImageTransparency = 0.3
            glowImage.ScaleType = Enum.ScaleType.Slice
            glowImage.SliceCenter = Rect.new(24, 24, 276, 276)
            glowImage.Parent = mainFrame
        end
    end
end)

-- Notification
Rayfield:Notify({
   Title = "Script Loaded",
   Content = "SavenHub is ready to use!",
   Duration = 5,
   Image = 4483362458,
})
